# -*- coding: utf-8 -*-
"""AI に渡す指示と、返してもらう形（schema）。

schema の決まり（ここでよく失敗する）:
  * object には必ず "type": "object" を書く（ネストの中にも）
  * array には必ず "items" を書く
  * required は本当に必須のものだけ。読めない項目を必須にすると AI が捏造する
"""

# =============================================================== L1: 請求書を読む
EXTRACT_PROMPT = """あなたは請求書のPDFから、仕訳を起こすための構造化データを取り出す担当です。

## 大前提
- **文字列・真偽値の項目は省略せず、必ずそのキーを含めてください。** 読めなければ文字列は空文字
  `""` にする（真偽値は最も妥当なほうを判定する）。**絶対に値を捏造しないこと。**
- **数値項目（total_incl_tax・amount_incl_tax など）は、書類に本当に存在しないときだけ省略してよい。**
  無理に 0 を入れないこと。推測でしか埋められない値は省略し、confidence を下げ needs_review を true にする。
- 1つのPDFに複数の帳票（請求書・納品書・明細書・社内の集計表）が綴じられています。**帳票ごとに分けて**ください。
- 90度回転しているページがあります。回転していても読んでください。

## documents（帳票）
- doc_type は 請求書 / 請求内訳書 / 納品書 / 部品請求明細書 / 御買上明細書 / 見積書 / 領収書 /
  受領書 / カード利用明細 / 振込依頼書 / 内部集計表 / その他 から選ぶ
- is_internal_worksheet: 社内で作った集計表・配分表なら true（**仕訳の最重要ヒント**）
- is_supporting_doc: 請求の根拠にならない参考添付なら true
- total_incl_tax は伝票の突合キーです。**読めたら必ず入れる**（税抜しか無ければ税を足す）。
  請求書ではない帳票（納品書・受領書など）で総額が無いものは、**省略してください。作らないこと**。
  同じ支払を2枚以上が同じ金額で書いているときも1枚だけに入れる（下の節）
- withholding_tax（源泉徴収税額）がある場合は必ず入れる
- handwritten_account_codes: 手書きの3段コード（下の「最重要」参照）を**そのまま**配列で
- 日付は YYYY-MM-DD
- **vehicle_no / vehicle_plate: その帳票が「どの車のものか」。ここが最重要です。**
  整備・車検の請求書は**1台につき1枚**で、車番は明細行ではなく**帳票の見出しや宛名の横**に
  書かれています（例「富山 100 き 9999」「金沢9999」「車輌No.0088」）。
  `vehicle_no` には**4桁の社内車番**だけ（0532 のように頭の0も落とさない）、
  `vehicle_plate` には登録番号をそのまま入れる。
  車番が分かるとセグメント（通運349／営業249／検修199 など）が車両マスタで確定します。
  **同じ整備でも車が違えばセグメントが変わる**ので、ここを落とすと当てられません。
  1枚に複数の車が混ざっているときは帳票側は空にして、各明細行の `vehicle_no` に入れる。

### 同じ支払を2枚以上が書いているとき — 総額は1枚だけに入れる

**これがいちばん多い間違いです。** 同じ支払について、同じ金額の書類が何枚も綴じられます。
全部に total_incl_tax を入れると、下流で金額を足したときに**2倍・3倍になります。**

同じ支払を書いている2枚の例。

- **カード会社の明細 と 実際に納めた会社の明細**（例: トヨタファイナンス 23,228円 と
  松村物産 23,228円）。請求してくるのはカード会社の1枚だけ
- **鑑（請求書）と その内訳**（請求内訳書・御買上明細書・部品請求明細書）
- **請求書 と 受領書**（受領書は受け取った証拠で、請求そのものではない）
- **同じ請求書の写しが2枚**

こうしてください。

1. **請求してくる1枚**（お金を払う相手が発行した、請求書の顔をしている帳票）だけ
   `total_incl_tax` を入れて `is_supporting_doc: false` にする
2. **残りの同じ金額の帳票**は `is_supporting_doc: true` にして
   **`total_incl_tax` を省略する**。明細（line_items）はそのまま全部出す。
   明細は仕訳を割るのに使うので落とさないこと
3. カード明細のように「請求元と実際に納めた会社が違う」ときは、
   実際に納めた会社の名前を明細の `actual_supplier_name` に入れる

**金額が同じでも別の支払なら、両方に total_incl_tax を入れてください。**
たとえばA社12,164円とB社12,164円が偶然同額で並んでいるだけなら、
これは2つの支払です。同じ支払かどうかは、**発行者のつながり**（カード会社と加盟店、
本社と支店）と**品目が同じものを指しているか**で判断します。金額の一致だけで決めないこと。

## line_items（明細行）
- row_type は detail / subtotal / total / carryover / payment / note / tax_summary。
  **合計行・小計行・繰越行・入金行を detail にしない**（二重計上の原因になります）
- **社内の集計表（is_internal_worksheet が true）の小計行は、1行も落とさず
  `row_type: "subtotal"` で出してください。** 「計（本社 事業 軽油）」
  「科目別集計 通運/備消品費/福井」のように、事業所名と経費区分が入った小計行です。**これが仕訳の割り方そのもの**で、
  ここを落とすと事業所ごとの配分が復元できなくなります。名前は書かれたまま
  （事業所名・経費区分名を含めて）item_name に入れる。いちばん外側の総計だけ
  `row_type: "total"` にする
- amount_incl_tax は detail 行では**読めたら必ず入れる**（税抜しかなければ計算する）。読めなければ省略する
- office: 請求書に書かれた事業所名（本社 / 福井 / 富山 / 魚津 / 伏木 / 高岡 / 青海 / 金沢 など）をそのまま
- cost_segment: 請求書に書かれた経費区分（管理 / 事業 / 通運 / 営業 / 検修）をそのまま
- purpose_category: 品名から導く用途区分。ガソリン / 軽油 / 軽油引取税 / 洗車 / 車検手数料 /
  整備・修理 / 部品 / 高圧ガス / 容器使用料 / 消耗品 / 事務用品 / 運賃 / 鉄道運賃 / 通行料 /
  保険 / 広告 / クリーニング / リース料 / 賃借料 / 電気 / 水道 / 通信 / その他。無ければ自由記述
- actual_supplier_name: カード明細などで請求元と実際の供給元が違うとき、実際に納めた会社名
- vehicle_no: 社内車番（**4桁**。0532 のように頭の0も落とさない）。
  vehicle_plate は登録番号そのまま。行ごとに車が違う明細（カード明細・燃料）では必ず入れる
- handwritten_account_code: その行に対応する手書き3段コード（下の「最重要」参照）
- evidence: そう読んだ根拠になる原本の短い引用（20字以内）

## 最重要 — 手書きの3段コードを絶対に見落とさないこと

経理担当者が請求書に赤や青のペンで書き込む **`3-639-21` のようなコード**が、
この仕組みでいちばん効く手がかりです（このコードがある行は仕訳の正解率が88〜89%）。
**見落とすと精度が大きく落ちます。**

**形**: `{1桁}-{3桁}-{1〜2桁}`。最初の桁は 1〜5 のどれか。
例: `3-639-21` / `5-624-8` / `1-727-1` / `4-623-6` / `3-639-31` / `5-679-8`

**どこに書いてあるか**（この順で必ず探すこと）

1. **「請求金額」「合計」「今回御請求金額」「御買上額合計」の欄のすぐ横・下・欄外** ← ここが最も多い
2. 明細行の左端や右端の余白
3. 帳票のいちばん上（社名の近く）や、いちばん下の余白
4. 丸で囲まれていたり、二重線が引かれていたりする

**大事な性質**

- **ほとんどの請求書に1つは書かれています。** 「無い」と結論する前に、
  合計欄の周りと、ページ全体の余白をもう一度見てください
- 燃料の請求書のように、**1枚に2つ以上**書かれていることがあります
  （例: 軽油に `3-639-21`、軽油引取税に `3-639-31`）。全部拾ってください
- 印刷ではなく**手書き**です。かすれていたり、数字が続けて書かれていたりします。
  読めた数字だけを書き、読めない桁があれば書かずに handwritten_memo に見えたままを残す
- 見つけたら **documents の `handwritten_account_codes` に全部**入れる。
  どの明細行に対するものか分かるなら、その行の `handwritten_account_code` にも入れる
  （合計欄に1つだけ書かれている場合は documents 側だけでかまいません）

赤いチェック（✓）や日付の書き込み（`3/6` など）も、見つけたら handwritten_memo に残してください。

## ID の付け方
doc_id は "D01" から、line_id は "L001" から通し番号。同じPDF内で重複させないこと。
"""

EXTRACT_SCHEMA = {
    # 任意項目が多すぎるとAnthropicのstructured outputが「no structured output」で
    # 全滅する（実測: 39個で発生）ため、文字列・真偽値・配列項目は required 化した
    # （空文字/最も妥当な判定で必ず埋まるため）。書類に本当に無いことがある数値項目だけは
    # 元通り任意項目のまま（null型は使わない。null型配列はAnthropic側で拒否された実績あり）。
    "type": "object",
    "properties": {
        "documents": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "doc_id": {"type": "string"},
                    "page_from": {"type": "integer"},
                    "page_to": {"type": "integer"},
                    "doc_type": {"type": "string"},
                    "is_internal_worksheet": {"type": "boolean"},
                    "is_supporting_doc": {"type": "boolean"},
                    "vendor_name": {"type": "string"},
                    "buyer_office": {"type": "string"},
                    "issue_date": {"type": "string"},
                    "closing_date": {"type": "string"},
                    "invoice_no": {"type": "string"},
                    "prev_balance": {"type": "number"},
                    "payment_received": {"type": "number"},
                    "total_incl_tax": {"type": "number"},
                    "total_excl_tax": {"type": "number"},
                    "withholding_tax": {"type": "number"},
                    "gasoline_tax_total": {"type": "number"},
                    "vehicle_no": {"type": "string"},
                    "vehicle_plate": {"type": "string"},
                    "handwritten_account_codes": {"type": "array", "items": {"type": "string"}},
                    "handwritten_memo": {"type": "string"},
                    "extraction_confidence": {"type": "number"},
                    "needs_review": {"type": "boolean"},
                },
                "required": [
                    "doc_id", "page_from", "page_to", "doc_type",
                    "is_internal_worksheet", "is_supporting_doc", "vendor_name",
                    "buyer_office", "issue_date", "closing_date", "invoice_no",
                    "vehicle_no", "vehicle_plate",
                    "handwritten_account_codes", "handwritten_memo",
                    "extraction_confidence", "needs_review",
                ],
            },
        },
        "line_items": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "line_id": {"type": "string"},
                    "doc_id": {"type": "string"},
                    "line_seq": {"type": "integer"},
                    "row_type": {"type": "string"},
                    "transaction_date": {"type": "string"},
                    "item_name": {"type": "string"},
                    "quantity": {"type": "number"},
                    "unit_price": {"type": "number"},
                    "amount_excl_tax": {"type": "number"},
                    "tax_amount": {"type": "number"},
                    "amount_incl_tax": {"type": "number"},
                    "tax_rate": {"type": "number"},
                    "gasoline_tax": {"type": "number"},
                    "office": {"type": "string"},
                    "cost_segment": {"type": "string"},
                    "purpose_category": {"type": "string"},
                    "actual_supplier_name": {"type": "string"},
                    "vehicle_no": {"type": "string"},
                    "vehicle_plate": {"type": "string"},
                    "handwritten_account_code": {"type": "string"},
                    "confidence": {"type": "number"},
                    "evidence": {"type": "string"},
                },
                "required": [
                    "line_id", "doc_id", "line_seq", "row_type", "transaction_date",
                    "item_name", "office", "cost_segment", "purpose_category",
                    "actual_supplier_name", "vehicle_no", "vehicle_plate",
                    "handwritten_account_code", "confidence", "evidence",
                ],
            },
        },
        "notes": {"type": "string"},
    },
    "required": ["documents", "line_items"],
}


# =============================================================== L2: 仕訳行に割る
SPLIT_PROMPT = """請求書から、**起こすべき仕訳行**を決めてください。

## 会計上の原則（実データ1,017伝票で確認。98%がこの粒度）
**1仕訳行 ＝ 購入部門 × 勘定科目 × 補助科目 × セグメント × 摘要 が一意に決まる最小単位**
摘要は「車番＋作業/品目名＋取引先」で書かれます。

# まず分岐 — `社内配分表` があるかどうかで読み方が変わる

| | 何を見るか | 考え方 |
|---|---|---|
| `社内配分表` がある | **配分表** | 表の小計行が仕訳行そのもの → **A** |
| `内訳帳票` がある | **内訳の1枚1枚** | 1枚が1行（1台1枚の整備請求書） → **A'** |
| どちらも無い | 請求書の明細 | 車番 → 事業所・区分 → 費目 で割る → **B** |

両方あるときは `社内配分表` を優先します。

---

# A. `社内配分表` があるとき

配分表は経理担当者が自分で書いた表で、**すでに仕訳の形に割ってあります。**
請求書の明細より配分表を優先してください。

**表の見た目が「明細行」か「小計行」かは関係ありません。**

- 燃料の表: カード番号ごとの明細は、同じ部門・同じ科目の取引が並んでいるだけ。
  会計上は区別されないので、**部門×油種の「計」行が1仕訳行**になる
- 部門別配賦表: 各セルが 部門×箇所×科目 の交点。**セル自体が1仕訳行**になる
- 軽油は「軽油代」と「軽油引取税」で補助科目が違う（6392 / 6393）ので**別の行**
- **いちばん外側の総計行だけは行にしない**（二重計上になる）

### 配分表に無い費目は、同じ比率で割る

```
燃料の伝票（2026年1月・2,151,187円）

社内配分表   計(本社事業軽油)778,330  計(本社通運軽油)246,147  計(福井通運軽油)107,827
            計(富山通運軽油)213,248  計(富山営業軽油)217,870
明細        軽油代 / 軽油引取税 / アドブルー の3費目

正解は14行 = 3費目 × 事業所区分。配分表に載っているのは軽油代の5本だけだが、
軽油引取税とアドブルーも**同じ事業所区分に、同じ比率で**分かれている
```

配分表の合計が `voucher_amount` に届かなくても構いません（上の例では引取税のぶんが足りない）。
**足りないぶんは、配分表と同じ事業所・区分の並びに、比率で割り当ててください。**

```
消耗品の伝票（2026年1月・56,790円）→ 8行。配分表がそのまま答えになっている例
    科目別集計 検修/事務用消耗品費/富山    科目別集計 検修/備消品費/富山
    科目別集計 通運/備消品費/福井        科目別集計 通運/備消品費/本社
    科目別集計 管理/雑費共/本社          …（8行）
同じ「消耗品」でも、事業所と経費区分が違えば別の行になる
```

---

# A'. `内訳帳票` があるとき — 1枚が1行

`内訳帳票` は、**この伝票が「鑑1枚＋内訳N枚」でできている**ときに渡されます。
内訳N枚の総額を足すと伝票金額になります。**その1枚1枚が仕訳行の単位です。**

整備・車検の請求書は**1台につき1枚**なので、これがそのまま「1台1行」になります。

```
整備の伝票（2026年1月・192,016円）＝ 鑑1枚
  内訳帳票3枚  93,016 / 86,900 / 12,100  → 正解もこの3行（1台1枚）
```

**金額は内訳帳票の総額をそのまま使ってください。** 鑑の金額を行数で割ってはいけません。
1枚の中がさらに費目で分かれるとき（車検一式など）は、その枚をさらに下の B の規則で割ります。
そのときも**その枚の総額の中で割る**（他の枚に食い込ませない）。

---

# B. `社内配分表` も `内訳帳票` も無いとき — 分ける軸は3つ。車番 → 事業所・区分 → 費目

**① 車番**
整備・車検の請求書は1台につき1枚です。車番は明細行ではなく**帳票の見出し**に
書かれているので、`source_lines` の `この帳票の車番` を見てください。
行に `車番` が無くても `この帳票の車番` があれば、その行はその車のものです。

**`source_lines` に車番が無くても、そこで諦めないでください。**
渡したページ画像の**見出し・宛名の横・車輌欄**に書かれていることがあります
（例「金沢 100 か 9999」「車輌No.0088」「福井9999」）。整備・車検・点検の行では必ず探し、
見つけたら**その行の `vehicle_no` に4桁で入れてください**（0532 のように頭の0も落とさない）。

**車番が入っているかどうかで結果が変わります。** 車番があれば車両マスタで部門と
セグメントが確定しますが、無いと品名からの推測になります。実データでは、同じ請求書でも
車番を拾えた回は正解、拾えなかった回はセグメントが別の値に入れ替わりました。
**`vehicle_no` は必ず返してください**（本当に無いときだけ空文字）。

**② 事業所・区分**
車番が付かない明細（燃料・消耗品・事務用品）でも、**1枚の請求書が複数の事業所ぶんを
まとめて請求していることがあります。** 各明細の `事業所`（本社/福井/富山 など）と
`経費区分`（管理/事業/通運/営業/検修）が違えば別行です。
**請求書の宛先事業所を全行に付けて1行にまとめてはいけません。**

**③ 費目**
①②が同じでも、勘定科目・補助科目が違えば別行です。

実データの正解はこうなっています。**金額が不揃いなことに注目してください。**

```
整備の伝票（2026年4月・251,515円）→ 3行（3台。点検と修理なので科目はどれも整備費）
    1台目 チェックランプ点灯      193,259
    2台目 尿素電気ヒータ交換       45,056
    3台目 3ヶ月点検               13,200

車検の伝票（2026年2月・428,294円）→ 12行（複数台 × 費目）
    車検整備は 427000、重量税は 428190、自賠責は 426500、手数料は 426200 と
    **1台の車検が科目ごとに分かれる**
```

**点検・修理は1台1行、車検は1台5行**です。違いは科目の数で、車検には
重量税（租税公課）・自賠責（保険料）・印紙（租税公課）・手数料（支払手数料）という、
整備費とは別の科目が必ず含まれるからです。

### 別行にする（合算しない）
- **車番が違う**（同じ作業でも車両ごとに別行）。帳票が別なら別の車と考える
- **事業所・経費区分が違う**（同じ品名でも別行）
- 同じ車番でも作業内容が違う（別の入庫・別の修理は別行）
- 車検一式は **整備 / 自賠責 / 重量税 / 印紙 / 検査手数料** で必ず別行。
  **勘定科目が違うため**で、同じ車の同じ請求書でも分ける。
  「車検諸費用」「法定費用」のように**まとめた名前の1行を作ってはいけません。**
  実際にあった誤り: 自賠責・印紙・手数料を足して「車検諸費用」の1行にした。
  正解は3行。**科目が 426500 / 428190 / 426200 と全部違うため**
- 燃料代と軽油引取税は補助科目が違うので必ず別行
- 物品本体と工賃・作業料は科目が違えば別行
- 品目・役務の名称が違い、科目も違う

### 1行にまとめる
- **同じ車番 × 同じ事業所 × 同じ科目になる明細は1行**。整備の請求書にある
  「作業工賃」「部品代」「副資材費」「診断機使用料」「車体納車」は
  どれも整備費で同じ科目なので、**1台ぶんを1行にまとめる**
- 同じ品目/作業が複数枚・複数行に分かれているだけ（納品書番号の違いは無視）
- 同じ品目の数量違い・日付違いの繰り返し
- **送料・配送料・運賃・事務手数料などの付随費用は、その品物の行に含める。**
  単独の行にしないこと（実績1,759行のうち送料が単独行なのは1行だけ）。
  実際にあった誤り: 部品代と送料を2行に分けた。正解は送料を部品代に含めた1行

### 行にしない
合計行・小計行・繰越行・入金行・値引行（値引は本体金額から差し引く）

### 判断の手順
1. 明細を全部拾う（合計・繰越・入金は除く）
2. 各明細に「車番」「事業所・区分」「品目/作業名」を付ける
3. **その3つが同じものだけを合算**してグループを作る
4. グループ単位で1仕訳行にする
5. 行の合計が `voucher_amount` と一致するか検算する

**迷ったら分けるほうを選んでください。** 実データでは細かく分かれている側が正解のことが多い。

---

# 共通の決まり

## 金額を等分してはいけない

**読めなかった金額を、行数で割って埋めることは禁止です。**
実際にあった誤り: 3台ぶんの整備代を、総額を3で割って 1円単位まで等分した。
正解は台ごとにまったく違う金額で、1台が総額の8割を占めていた。

金額は必ず紙に書かれている数字から取ってください。行の数は分かるのに金額が読めないときは、
**分けずに1行のままにして** `sum_note` に「○台ぶんだが台別の金額が読めない」と書いてください。
等分した行は合計が合ってしまうので検算をすり抜け、誤ったまま確定します。

禁止しているのは**根拠のない等分**です。Aの `社内配分表` のように紙に配分が書かれていれば、
その比率で割るのは正しいやり方です。

## 過去の割り方（`社内配分表` があればそちらが優先）

`直近5伝票の行テンプレート` には、この支払先の過去の伝票が**実際にどう割られていたか**が
4項目つきで入っています。**何行に・どういう単位で割っているか**をつかんで、今回も合わせてください。
先月が5つの部門に分かれていたなら、今月も分かれるはずです。

**金額は真似しない**（毎月違う）。真似するのは割る単位と行の名前の付け方です。
過去の実績が無い支払先は原則だけで判断します。

## 必ず守ること
- 行の金額合計を `voucher_amount` に一致させる。合わなければ sum_matches を false にして理由を書く
- 各行に `source_line_ids`（元になった明細のID）を書く。**推測でIDを書かない**。
  特定できないときは空にして source_relation を "伝票合計から" にする
- source_relation は 対応 1:1 / 結合 N:1 / 分割 1:N / 伝票合計から のいずれか
- label は「車番 品目・作業名 取引先」の形（例: 富山9999 タイヤ購入 ㈱○○）
- **reasoning に、A と B のどちらで読んだか・どの階層をなぜ選んだかを必ず書く**
"""


SPLIT_SCHEMA = {
    "type": "object",
    "properties": {
        "reasoning": {"type": "string"},
        "lines": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "label": {"type": "string"},
                    "item_label": {"type": "string"},
                    "vehicle_no": {"type": "string"},
                    "amount": {"type": "integer"},
                    "confidence": {"type": "number"},
                    "source_line_ids": {"type": "array", "items": {"type": "string"}},
                    "source_relation": {"type": "string"},
                },
                # vehicle_no を required にする。任意にしておくとAIが省略し、
                # セグメントを車両マスタで決められなくなる（実データで249/349が反転した）
                "required": ["label", "item_label", "vehicle_no", "amount", "confidence"],
            },
        },
        "sum_matches": {"type": "boolean"},
        "sum_note": {"type": "string"},
    },
    "required": ["lines"],
}


# =============================================================== L3: 4項目を決める
CLASSIFY_PROMPT = """各仕訳行に、購入部門(CDJS300) / 勘定科目(CDJS301) / 補助科目(CDJS302) /
セグメント(CDJS303) の4つのコードを付けてください。**入力の行数と同じ数だけ、同じ順番で**返します。

## 判断の順番（上から試して、決まったところで確定）

**1. 手書きコード**（最優先）
3段コード「第1段-第2段-第3段」の意味:
- 第1段 = セグメント: 1→449(管理) 2→249(事業) 3→349(通運) 4→149(営業) 5→199(検修)
- 第2段 = 勘定科目（`手書きコードの変換規則` を引く）
- 第3段の1桁目 = 補助科目、2桁目 = 箇所: 1→0001本社 3→0002福井 5→0005高岡
  6→0004富山(魚津) 7→0006富山-青海 8→0007富山-検修

**2. 車番** → `車両マスタ` → 無ければ過去実績の同じ車番

**3. `社内配分表`**（あれば、事業所と経費区分について**車番の次に強い**）

経理担当者が自分で書いた配分表です。**小計行の名前に事業所と経費区分がそのまま入っています。**

```
科目別集計 営業/備消品費/福井  → 経費区分「営業」=セグ149、事業所「福井」=部門0002
計（本社 事業 軽油）          → 事業所「本社」=部門0001、区分「事業」=セグ249
```

金額が一致する行があれば、**その名前の事業所・経費区分をそのまま使ってください。**
下の4の「請求書の記載」より確かです（配分表は経理担当者が仕訳のために書いたものなので）。

**4. 請求書の記載**（請求書そのものに書かれていた文字。コードに変換済みではありません）

| 記載 | 対応 | そのまま使ったときの的中 |
|---|---|---|
| 事業所「本社」 | 部門 0001 | |
| 事業所「福井」 | 部門 0002 | |
| 事業所「富山」「魚津」「伏木」 | 部門 0004 | |
| 事業所「高岡」 | 部門 0005 | |
| 事業所「青海」 | 部門 0006 | |
| 経費区分「通運」 | セグ 349 | 98% |
| 経費区分「営業」 | セグ 149 | 100% |
| 経費区分「検修」 | セグ 199 | 100% |

**そのまま変換してはいけない例（実データで確認済み）**
1. 事業所「富山」＋検修の作業 → 部門は0004ではなく **0007**（富山検修所。実データ30件）
2. 事業所「金沢」 → 部門0003ではなく **0001**（本社。実データ12件）
3. 事業所「富山」でも本社が使うものを買っていれば **0001**（実データ7件）

**用途区分で勘定科目・補助科目を決めてはいけません。**
用途区分（賃借料・消耗品・資材など）は請求書側のざっくりした分け方で、勘定科目名ではありません。
部門・セグメントを考えるヒントにだけ使ってください。
悪い例:「25tラフタークレーン手配」を用途区分「賃借料」だからと426400にした。
正解は426800の補助6461（外注作業）。**買ったものが何かで決めます。**

※ 請求書に「管理」「事業」と書かれていた経費区分は、当たらないので**渡していません**。
  `経費区分` が空でも「管理でも事業でもない」という意味ではありません。

**5. 請求書品名の対応** `この支払先の請求書品名と4項目の対応` に同じ品名があれば、その4項目

**6. 支払先の過去実績** から、**何を買ったかが同じ行**を探す（字面ではなく内容）
- 値が割れていたら `ルール期=新` の行を優先。無ければ件数の多いほう
- 割れていたことを根拠に「1〜5月は◯◯／6月以降は△△」と両方書く

**7. 全社の品目ルール** 支払先に実績が無いときだけ使う

**8. 管理／事業の切替を必ず確認**
セグ449(管理)の行は科目が **45x系**、それ以外の行は **42x系** でなければなりません。
合っていなければ `管理と事業の科目対応` で相手側に置き換えます。
（例: 制服 事業425200 ↔ 管理451015 / 車検整備 427000 ↔ 451175 / 自賠責 426500 ↔ 451140）
同じ伝票でも行ごとにセグメントが違えば科目も変わります。

**9. それでも決まらないとき**
品目の性質から最も近い科目を選び、補助科目は 0000、部門は請求書の宛先、
セグメントは用途（管理業務なら449、輸送・車両なら349/249）から推定して、confidence を 0.5 以下にする。
**項目を省くのは、科目の候補が本当に絞れないときだけ**です（省いた行は必ず人が確認します）。

## 気をつけること
- 部門0004（富山営業所）と0007（富山検修所）は別。検修は 0007/199
- 燃料: 軽油代6392 / 軽油引取税6393 / ガソリン6391 / アドブルー6394（管理側は451169の7391等）
- 車検一式: 整備427000 / 自賠責426500 / 重量税・印紙428190 / 検査手数料426200-6351
  （管理側は451175 / 451140 / 451219 / 451123-7351）
- マスタに無い＝不明ではない。車番が車両マスタに無くても、過去実績で必ず埋めにいく

## 出力
各行に `根拠`（なぜそのコードにしたか。参照した過去の伝票Noも書く）、
`根拠種別`（手書きコード / 車両マスタ / 車番の実績 / 請求書の記載 / 請求書品名の対応 /
支払先の過去実績 / 全社の品目ルール / 管理事業の切替 / 推定）、`confidence`(0-1) を付けます。
"""

CLASSIFY_SCHEMA = {
    "type": "object",
    "properties": {
        "lines": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "no": {"type": "integer"},
                    "CDJS300": {"type": "string"},
                    "CDJS301": {"type": "string"},
                    "CDJS302": {"type": "string"},
                    "CDJS303": {"type": "string"},
                    "根拠": {"type": "string"},
                    "根拠種別": {"type": "string"},
                    "confidence": {"type": "number"},
                },
                "required": ["no", "confidence"],
            },
        }
    },
    "required": ["lines"],
}


# =============================================================== S7: 摘要を書く
SUMMARY_PROMPT = """会計システムの摘要欄に入れる文章を、各行について書いてください。

- 書き方: 「月表記 車番 品目・作業名 取引先名」
- **40文字以内**
- 取引先名は半角カナ＋㈱（例: ㈱石川ﾄﾖﾍﾟｯﾄｶﾛｰﾗ）
- 例: `金沢9999 車検整備 ㈱石川ﾄﾖﾍﾟｯﾄｶﾛｰﾗ`

`この支払先の過去の摘要` があれば、**同じ書き方を真似**してください。
入力の行数と同じ数だけ、同じ順番で返します。
"""

SUMMARY_SCHEMA = {
    "type": "object",
    "properties": {
        "lines": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "no": {"type": "integer"},
                    "摘要": {"type": "string"},
                },
                "required": ["no", "摘要"],
            },
        }
    },
    "required": ["lines"],
}
