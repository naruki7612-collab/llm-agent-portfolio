# 匿名化について

このリポジトリのコードは業務案件の成果物です。公開にあたり、機械的な置換で
識別情報を架空の値に差し替えています。**処理ロジックは一切変更していません。**

## 置き換えたもの

### 企業名

| 元 | 置換後 |
|---|---|
| 顧客A（リース業） | アルファリース株式会社 / `ALI` |
| 顧客B（卸売業） | ベータ商事 / `BETA` / `beta` |
| 顧客C（不動産管理） | ガンマ管理株式会社 |
| 顧客D（保険代理店） | デルタ保険 |
| 顧客E（設備・商材） | `EPSILON` / `epsilon` |
| 顧客F（広告・メディア） | `ZETA` |
| 顧客G | シータ |
| 所属先 | 自社 / `MyCompany` |
| 社内エージェント基盤 | `AgentPlatform` / `agent_platform` |
| 基盤SDK（`*_tools`） | `agent_sdk` |
| 取引先（設備） | サンプル設備 |
| 取引先（重車輛工業） | カモメ重車輛工業（輛/両/輌の3表記とも） |
| 取引先（ガス・2社） | ミドリガス / ヒカリガス |
| 取引先（自動車） | カナタ自動車 |
| 取引先（商事） | ナギサ株式会社 |
| 取引先（運送） | ハヤテ急便 |
| 取引先（エネルギー） | ソラチエネルギー |

### 人名・メールアドレス

- 実在する担当者名 → `担当A` 等
- `@（所属先ドメイン）` → `@example.com`
- プロンプト内の例示に使われていた実在企業のドメイン
  （`@***.co.jp` など）→ `@company-a.example.jp` 形式

### 企業を特定しうる属性

社名を伏せても特定できてしまうため、社名以外の属性も置換しています。

| 置換後 | 元の中身 |
|---|---|
| `<CORP_NUMBER>` | 法人番号 |
| `<ADDRESS_1..3>` | 本店・事業所の所在地 |
| `<TEL_1..2>` | 電話番号 |
| `<BANK>` | 取引銀行 |
| `<CUSTOMER_CODE>` | 顧客コード |
| `<DEPT>` | 部署名 |
| 代表者A / 役員A・B / 監査役A / 担当B | 役員・担当者の氏名 |

### 実在の車両ナンバー・物件

4桁の車番と、物件の「町名＋地番」は架空値に差し替えています。
陸運局・営業所・市区町村の地名は、事業所コードの対応表や台帳の列位置判定に
使われるロジックの一部のため残しています（特定の企業には結び付きません）。

置換にあたっては、コメントに残した実測の論点が読めなくならないようにしています。
たとえば異体字の実例は輛/両/輌の3表記とも同じ架空社名に寄せ、
「同業種語を含む別業者は文字列類似度が高く出る」の実例は2社とも「ガス」を残し、
地番の桁誤りの実例は桁数の差を保った値にしています。

### 識別子

実IDはすべて用途の分かるプレースホルダに置換しています。

| 置換後 | 元の中身 |
|---|---|
| `<RAW_DB_ID>` | 情報集約の統合DB |
| `<PEOPLE_DB_ID>` | 人物辞書DB |
| `<DOMAIN_MAP_DB_ID>` | ドメイン→企業の対応表DB |
| `<SYNC_STATE_DB_ID>` | 同期状態の保存先DB |
| `<RAG_FILE_UUID>` | RAG登録ファイル |
| `<INDEX_XLSX_UUID>` | 索引Excel |
| `<MAIL_FOLDER_ID_1..3>` | メールフォルダ |
| `<DRIVE_FOLDER_ID>` / `<ONEDRIVE_ITEM_ID>` | ストレージ上のフォルダ・ファイル |
| `<LABEL_ID_*>` | Gmail のラベルID |
| `<UUID>` | 上記以外のUUID |
| `<PLATFORM_URL>` | 基盤の共有URL |

### ローカルパス

| 置換後 | 元 |
|---|---|
| `<PROJECT_ROOT>` | 作業マシン上のプロジェクトディレクトリ |
| `<VAULT_ROOT>` | ノートアプリの保管庫 |
| `<HOME>` | ホームディレクトリ |

## 内容を書き直したファイル

置換では処理しきれず、**中身そのものを書き直した**ものが1つあります。

### `09-tools/gmail-label/gmail_label_agent.py`

元は個人の受信箱向けで、カテゴリ構成が作成者個人の事情に寄っていました。
汎用的なラベル構成（要対応 / 予定 / 取引 / 配信 / 通知 / その他）に作り直し、
子ラベルの生成も特定カテゴリ固定から設定で指定する形に一般化しています。
判定基準のプロンプトと動作確認サンプルも、それに合わせて差し替えました。

ラベルIDは実在の値だったため、プレースホルダにしています。
このリポジトリは匿名化後の状態から作り直しており、匿名化前の版はコミット履歴にも含まれません。

## 置き換えていないもの

- **業務ロジック** — 判定規則、勘定科目の対応、突合条件、しきい値
- **プロンプト本文** — 上記の識別情報を除く
- **コメント・docstring** — 設計意図と実測メモはそのまま

業種が推測できる程度の記述は残っています。特定の企業と結び付く情報は含めていません。

## 検証

置換後に以下を再スキャンし、いずれも0件であることを確認しています。

- 旧社名・旧人名・所属先名の文字列
- 実在ドメインのメールアドレス
- UUID 形式の文字列、ストレージの長ID
- APIキー形式（`sk-` / `ghp_` / `xox*-` / `AKIA*` / `AIza*`）
- `/Users/...` のローカル絶対パス
- 法人番号・電話番号・所在地・役員氏名
- 実在の取引先名、車両ナンバー、物件の地番

スキャンは作業ツリーだけでなくコミット履歴の全 blob に対しても実施しています。

なお元のコードにハードコードされた APIキー・トークンは**元から1件もありません**。

### 誤爆について

ローマ字の社名略称は、無関係な識別子の一部に埋まっていることがあります。
たとえば3〜4文字の略称が、日本語をローマ字表記した変数名
（`_KAKUNIN_YUSEN` や `GYOSHU_ITCHI_NG` のような、業務用語をそのまま綴ったもの）の
途中に偶然含まれる、という形です。単純な文字列置換をかけると、
社名と無関係な変数名まで壊れます。

そこで置換は、前後に英数字・アンダースコアが来ない場合だけマッチする
境界付き正規表現で行っています。置換後には、境界付きの再スキャンで
残骸が0件であることと、巻き込みが起きていないことの両方を確認しました。
